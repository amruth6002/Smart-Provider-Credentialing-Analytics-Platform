# Smart Provider Credentialing Analytics Platform Configuration

# Local AI Models Configuration
# The platform uses local AI models that don't require API keys!
# AI_CACHE_DIR=./models_cache  # Optional: specify where to cache downloaded models

# Database Configuration (if using external database)
# DATABASE_URL=your_database_url_here

# Debug Mode
# DEBUG=true

# Application Settings
# PORT=8501
# HOST=localhost